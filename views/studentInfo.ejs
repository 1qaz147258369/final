<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Insert title here</title>
<%-include('link.ejs')%>
</head>
<body>

	<div class="container">
		<%-include('top.ejs')%>

		<div class="row" style="margin-top: 10px">
			<div class="col-lg-2">
				<div class="list-group">
					<a href="s_index.ejs"class="list-group-item list-group-item-action">首页</a> 
					<a href="snamerecord" class="list-group-item list-group-item-action">缺寝记录</a> 
					<a href="s_MyInfo" class="list-group-item list-group-item-action">我的信息</a> 
				</div>
			</div>

			<div class="col-lg-6">

				<div class="card">
					<div class="card-header">用户信息</div>
					<div class="card-body">
						<div>
							<h2>用户信息如下：</h2>
							<div class="row">
								<div class="col-lg-6">
									<p>学号：${user.ssno}</p>
									<p>用户名：${user.sname}</p>
									<p>密码：${user.spassword}</p>									
									<p>性别：${user.ssex}</p>
									<p>学院：${user.institute}</p>
									<p>寝室楼栋：${user.building}</p>
									<p>寝室号：${user.room}</p>

								</div>

							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
	
</body>
</html>